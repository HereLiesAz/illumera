name: Finalize queue feature

on:
  push:
    branches:
      - feature/queue-lineup
    paths:
      - tools/finalize_queue_feature.py
      - .github/workflows/finalize-queue-feature.yml

permissions:
  contents: write

jobs:
  finalize:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: feature/queue-lineup
          fetch-depth: 0
      - uses: actions/setup-python@v6
        with:
          python-version: '3.13'
      - name: Apply final integration pass
        run: python tools/finalize_queue_feature.py
      - name: Commit final integration
        run: |
          rm tools/finalize_queue_feature.py .github/workflows/finalize-queue-feature.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git commit -m "Allow individual episodes in playback queue"
          git push origin HEAD:feature/queue-lineup
