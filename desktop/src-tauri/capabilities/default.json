{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main window. It needs no Tauri APIs: the sidecar is started from Rust.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
